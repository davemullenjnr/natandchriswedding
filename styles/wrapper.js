import styled from 'styled-components';
import { breakpoint, width } from '.';

export const DefaultWrapper = styled.div`
  margin: 0 auto;
  width: 88%;
  max-width: ${width.max}rem;

  @media ${breakpoint.md} {
    
  }

  @media ${breakpoint.lg} {
 
  }
`;